# Pixel Processor Basics

> **Status:** stub. Populated by a follow-up increment.

## Planned coverage

- What Pixel Processor is (a function-per-pixel pipeline) and how it differs from FX-Map (no recursion, but flexible sampling)
- Sample Color / Sample Gray (read from input images at arbitrary coords)
- Math-expression syntax in the Function Editor (vector ops, conditionals, loops)
- Common patterns: gradient generators, distance fields from a binary mask, sine-wave displacement, custom blends
- When to reach for Pixel Processor vs. FX-Map vs. a node chain

## Primary sources (to be ingested)

- Andrei's tutorials on Pixel Processor (links pending transcript pull)
- The 80.lv guide (Pixel Processor sections)
- ArtStation breakdowns where Andrei used Pixel Processor for custom node creation

## Citation format reminder

Every paragraph populated here must end with `*Source: Andrei Zelenco — <video title or article> — <URL>*`.
